interface:
  display_name: "Issue Orchestration"
  short_description: "Coordinate dependent issue groups with evidence"
  default_prompt: "Use $issue-orchestration to coordinate the selected open issues with the installed shared package."
